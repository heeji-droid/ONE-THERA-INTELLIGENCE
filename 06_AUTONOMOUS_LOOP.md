# 06 — AUTONOMOUS LOOP

이 시스템의 기본 모드는 **대기가 아니라 순환**이다.

사람이 자료를 주지 않아도 돈다. 사람이 질문하면 그 질문도 **같은 프레임워크를 통과한다.**
질문은 루프를 멈추는 것이 아니라 루프에 얹히는 것이다.

```
        ┌─────────────────────────────────────────────┐
        │                                             │
        ▼                                             │
   ① AGENDA          결핍을 찾는다 (coverage.py)      │
        │            "무엇이 비어 있는가"              │
        ▼                                             │
   ② SWEEP           스스로 수집한다                   │
        │            Tier 1→4, 결핍을 메우는 방향으로   │
        ▼                                             │
   ③ CASE STUDY      한 주제 = 한 케이스               │
        │            docs/cases/ 에 축적                │
        ▼                                             │
   ④ CONNECT         누적 저장소 전체에서 수렴을 본다   │
        │                                             │
        ▼                                             │
   ⑤ OPPORTUNITY     또는 "아직 아님 + 그 이유"        │
        │                                             │
        └─────────────────────────────────────────────┘
                          ▲
                          │
                   ⑥ ASK  사용자 질문 — 같은 프레임워크로 답한다
```

---

## ① AGENDA — 무엇을 볼지는 기계가 정한다

```bash
python3 tools/coverage.py --agenda data/insights/*.jsonl
```

`coverage.py`는 **많이 쌓인 곳이 아니라 비어 있는 곳**을 찾는다. 우선순위:

| 우선순위 | 결핍 | 의미 |
|---:|---|---|
| 100 | `blank` | 이 drive에 인사이트가 0건 — 정말 없는가, 안 본 것인가 |
| 90 | `no_fact_base` | Tier 1–2 없음 → **유행이지 기회가 아니다** |
| 85 | `no_consumer_voice` | Tier 4 없음 → **논문이지 시장이 아니다** |
| 70 | `no_cross_industry` | 뷰티 안에서만 관찰 → 구조인지 판별 불가 |
| 60 | `thin` | 독립 출처 부족 |
| 50 | `stale` | 120일 이상 미갱신 → 아직 유효한가 |

사람이 주제를 정하면 **확증편향이 들어온다.** 이미 관심 있는 것만 보게 된다.
결핍이 정하면 저장소가 스스로 균형을 잡는다.

## ② SWEEP — 스스로 수집한다

### 상시 비트 (매 사이클 반드시 돈다)

| 비트 | 어디 | Tier | 무엇을 |
|---|---|---|---|
| 규제 | FDA · Health Canada · EU CosIng | 1 | 새 규칙·금지·표시 의무 → `07.STRUCT` 후보 |
| 임상 | PubMed · ClinicalTrials.gov | 1 | 성분 근거의 변화 |
| 시장 | NIQ · Circana · Mintel 공개분 | 2 | 채널·카테고리 이동 |
| 업계 | WWD · Beauty Independent · BeautyMatter | 3 | 출시·경쟁·자금 |
| 소비자 | Reddit · Amazon/Sephora 리뷰 · TikTok | 4 | **원문 그대로.** 실패·좌절·이탈 서사 |
| 교차 | 결핍이 지목한 타 산업 | 1–4 | 같은 `human_drive`의 다른 발현 |

### 순환 초점 (사이클마다 하나씩)

`coverage.py`가 지목한 **최우선 결핍 1개**를 깊게 판다.
넓게 훑는 것은 상시 비트가 하고, 깊게 파는 것은 초점이 한다. 둘을 섞지 않는다.

### 수집 규율

- **Tier 4를 먼저 본다.** 진짜 기회는 대개 Tier 4에서 먼저 보이고 Tier 1–2에서 나중에 확인된다.
  순서를 뒤집으면 이미 아는 것만 확인하게 된다.
- 기존 저장소와 **중복을 확인한다.** 같은 원본을 재인용한 기사는 새 출처가 아니다.
- 찾지 못했으면 **찾지 못했다고 기록한다.** 빈손도 데이터다 (그 자리가 정말 비었다는 증거).

## ③ CASE STUDY — 축적의 단위

> **한 주제 = 한 케이스.** 케이스가 쌓이는 것이 이 저장소의 목적이다.

한 케이스의 최소 조건:

```
· Atomic Insight 5건 이상
· 서로 다른 Tier 2개 이상
· 사실(FACT)과 해석(INFER)이 분리되어 있을 것
· 결론이 "기회 없음"이어도 된다 — 단, 이유를 쓴다
```

**기회가 나오지 않은 케이스를 지우지 않는다.**
"여기는 비어 있지 않다"는 기록이 다음 사이클의 중복 탐색을 막는다.
`docs/cases/` → [형식](docs/cases/README.md)

## ④–⑤ CONNECT → OPPORTUNITY

케이스가 쌓이면 개별 케이스를 넘어 저장소 전체에서 수렴을 본다.
케이스 3개에서 같은 `human_drive`가 나왔다면, 그건 케이스 3개가 아니라 **신호 1개**다.

```bash
python3 tools/converge.py data/insights/*.jsonl
```

게이트를 통과하지 못하면 **기회를 만들지 않는다.**
대신 "무엇을 더 모으면 통과하는가"를 쓰고, 그것이 다음 사이클의 AGENDA가 된다.

## ⑥ ASK — 질문도 같은 길을 간다

사용자가 물으면 **새로 검색하기 전에 저장소를 먼저 본다.**

```
질문
 ├─ 저장소에 답이 있는가?          → 있으면 근거와 함께 답한다
 ├─ 부분적으로 있는가?             → 있는 것으로 답하고 없는 것을 명시한다
 └─ 없는가?                        → 그 자리를 AGENDA에 넣고 수집한 뒤 답한다
```

**즉답하지 않는다.** 저장소를 거치지 않은 답은 이 시스템을 쓰지 않은 답이고,
그렇게 답하면 그 답은 어디에도 축적되지 않는다.
질문에서 나온 인사이트도 전부 `data/insights/`에 들어간다 — 질문이 곧 수집의 계기다.

→ [`prompts/60_ASK.md`](prompts/60_ASK.md)

---

## 주기

| 사이클 | 하는 일 | 산출 |
|---|---|---|
| **매일** | 상시 비트 얕게 · Tier 4 중심 | Atomic Insight 5–15건 |
| **주간** | 최우선 결핍 1개 깊게 | Case Study 1건 |
| **월간** | 전체 수렴 재계산 · 기회 재평가 · 폐기 조건 만료 확인 | Opportunity 재정렬 |

## 멈추지 않는 것들

1. **빈손을 감추지 않는다.** 못 찾았으면 못 찾았다고 케이스에 남긴다.
2. **결핍이 사라지면 기준을 올린다.** 모든 drive가 채워지면 `THIN`을 높인다 — 시스템이 안주하지 않도록.
3. **폐기 조건이 만료된 기회는 그날 다시 연다.** 재검증하지 않은 예측은 자산이 아니라 부채다.
4. **오답을 지우지 않는다.** 틀린 인사이트·폐기된 기회가 남아 있어야 이 시스템의 오차를 측정할 수 있다.
